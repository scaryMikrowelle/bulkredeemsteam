# if you get rate limited, you can just restart the program after an hour and it will only continue with unused codes

# the symbols in front of the codes after running the program are symbolizing:
# # = done
# + = already owned
# ! = invalid key

# steam credentials
USERNAME = ''
PASSWORD = ''

# 2-FA
MFA_METHOD = 2
# 1 = No 2FA
# 2 = Accept on mobile
# 3 = Type in Code

# language settings default = english, only need to be parts but identifyable parts of error message
AccountOwnsProduct = 'account already owns'
KeyNotValid = 'is not valid'
TooManyAttemps = 'too many recent activation attempts'
